problems:

   mac: can't open when no file open — dialog is okay, but file never opens
   mac: opening with alt key works

next:

   environmental variables isMac (boolean) in both js & jsx
   functions fixSlashes(isMac) in both js & jsx

PC tests

   lastPath is wrong going into new-page.jsx
   it has no slashes, just all run together : C:UsersandyDesktoppixside.frsyncExample Pages

   √ test context menu
   √ test "image" in main and subfolders
   • test "dossier" in main and subfolders
     alert '', goes to windows root
   • make sure "new page" goes in correct folder

PC issues

   • new page button now opens template but tries to save in Links
     should be in same folder as last path

   • lastPath given to newPage is wrong

   • grid info wrong, even when I refresh the preferences.
     can I set the grid to match my info?

   √ canceling IMAGE causes fake image
     image was palced in correct spot
     created new links folder if needed
     SEE PHOTO FOR FIX IN JSX

   • make panel dark blue to differentiate, like old pitch deck
     make indicators same as current background, because there's a thin
     line at top that I can't get rid of
   • change px to rem everywhere

   • GRID 72 & 8 are reported by grid, but worked if I changed them
     — added alert to LS dump
     — report was incorrect
     gridline in status changes, see picture
     alt-click grid to open grid prefs

   ———————————————————————————————————————— won't fix today

   • need to add artboard widths to save report
   • after "save all" wrong file was in front
   • in "save all" written "4 files saved", should have been 2
   • filenames in save confirmation are wrong, footer was listed twice, "no missing"
   • vérifier fixed problem but didn't give report

   ———————————————————————————————————————— done

   √ button labels too big
   √ interrupt is timeout
   √ svija tools 3.0 —› change version to match Exchange version
   √ link at tech /svija-tools-2.0 not found

   √ dossier button has debugging alert
     — commented out

   √ URL.txt not found by site/cloud:save buttons after creation
     — not found even after restartc

   √ feedback screen has no close X

   √ title in manifest should have 3.0

   √ support should go to special link /tools3.0

   √ bottom links too dark
     — fixed

   √ mail links still broken
     — simplified new project email in case it's a quote problem
     — seem to be working — test
     — wrong charset & newlines see photo

   √ pixel is not 6e avant derneier — re-check (don't include views)

   √ extra dash to right of LS

   √ fixtilde in statusupdate uses hardcoded /Users/Main

   √ blank at startup, had to collapse & uncollapse to see
     x added timeout to reload panel
     — added alert in first script tag to see if panel is loaded at all
     — add Adove test panel to see if it opens, maybe copy headers

   √ fonts still wrong
  
   √ dossier opens ABOVE sync, not sync
     — alert: C:\Users\andy\Documents\pixside.fr
     — add \sync when it's a PC
     — need extgra \

   √ when return message is '', feedback should not be shown (saving with no url.txt)
     — now exit feedback function if no message

   √ overall very dark, not enough contrast

   √ title says tools 3.0 mouseover says 2.0
     — changed to 3.0 in tools.html

  √ font wrong
     — tried using TTF, adding second line with '\' instead

   √ clicking on ••• creates scrollbar
     — added overflow hidden universally

   √ LS alert only showed 2 lines, was cut off and couldn't be scrolled

   √ Dossier just went to Windows Explorer
     — added alert to see what's going on
     — changed to \ for PC

   √ site gives aucun project svija. if there's a sync folder, it should say please create URL.txt

   √ save gave alert URL.txt not present but then showed "undefined" message in spinner
     — changed "return" to "return ''" in save script

   √ C&R says "Merci de sauvegarder en premier" should be "Merci de sauvegarder avant de continuer"
     — fixed

   √ links to mailto: just did nothing
     — I'll have to configure a mail program

   √ panels disappear when no open document, so reopening last document is useless


NOTES ETC.
  • make a grid, and then press alt, then the same button in
    order to delete all the grids that you've created

  • store indicator states in localstorage


  • reset 0,0 automaticslly

  • set artboard rulers
    ruler  : View > Rulers > Show Rulers
    rulerCoordinateSystem  : View > Rulers > Change to Global Rulers

  • got message "not a svija site" when no page was open, but URL was known

  • check & repair error:

  Error 9024: Cannot modify a layer that is locked
  Line: 488
  -> var rec = obj •parent pathItems.rectangle( rNegTop, rLeft, rw...

———————————————————————————————————————— next

  if save is less than 1/2 second, no wait screen (set wait screen after 1/2 second?)

———————————————————————————————————————— to complete

  • fix check & repair
  • mouseover doesn't go away on click
  • missing url.txt should provoke an alert instead of showing the wrong site
  • nicer format of save complete message & increase delay before closing

———————————————————————————————————————— bugs

  xxx mismatch between open dialog title & actual folder
  
  after working on the vbca red round logo, I clicked the open button and saw vbca.svija.site
  but was really in centrefrance.levillagebyca.com
  clicking "dossier" had the same problem
  because url.txt was missing from cnetrefrance…

———————————————————————————————————————— possible additions

  • save svg w/o AI name, maybe w/prefix
    append artboard widths?`

  • set/get styles

  • Find and Replace : Edit > Find & Replace

  • save files sizes in a table and show them at all times

  • add alerts to the Adobe CEP panel to see what is actually added to the flyout menu

  • move "reload" to flyout menu

  • in save, check for /sync/sync/, __init__.py and signs of other trouble
    warning if folder is in iCloud

  • button to center obj on page, left, top, right, bottom
  • how to change height when double click on top tab, like adobe panels?
  • button to create/delete grid (popup with margins, number, gutter)


 -->

